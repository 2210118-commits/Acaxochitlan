import 'lugares_por_tipo_page.dart';

class HotelesPage extends LugaresPorTipoPage {
  const HotelesPage({super.key})
      : super(
          tipo: 'hotel',
          titulo: 'Hoteles',
        );
}
